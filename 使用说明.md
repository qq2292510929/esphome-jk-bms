# ESP32-32E JK-BMS 显示屏项目使用说明

## 已完成的配置

根据提供的引脚配置图，我已经创建了 `esp32-32e-jkbms-display.yaml` 配置文件，引脚映射如下：

### SPI 引脚 (LCD屏幕)
| 功能 | ESP32-32E引脚 | 说明 |
|------|--------------|------|
| TFT_CLK/SPI_SCK | GPIO14 | 时钟 |
| TFT_MOSI/SPI_SDI | GPIO13 | 数据输入 |
| TFT_MISO/SPI_SDO | GPIO12 | 数据输出 |
| TFT_CS | GPIO15 | 片选 |
| TFT_RS/D/C | GPIO2 | 数据/命令选择 |
| TFT_RST/RES | GPIO4 | 复位 (使用备用引脚，原图连接到EN) |
| TFT_BL | GPIO21 | 背光 |

### 其他引脚
| 功能 | ESP32-32E引脚 | 说明 |
|------|--------------|------|
| BOOT_KEY | GPIO0 | 页面切换按钮 |
| LED_RED | GPIO22 | RGB LED红色 |
| LED_GREEN | GPIO16 | RGB LED绿色 |
| LED_BLUE | GPIO17 | RGB LED蓝色 |

## 需要您修改的部分

### 1. 修改BMS蓝牙MAC地址

在 `esp32-32e-jkbms-display.yaml` 文件第5行：

```yaml
mac_address: C8:47:8C:E1:E2:AA  # 需要根据实际BMS蓝牙地址修改
```

**如何找到您的BMS蓝牙地址：**
- 打开手机蓝牙扫描
- 寻找名称类似 "JK-BMS"、"JK" 或您具体型号的设备
- 记录其MAC地址，格式为 `XX:XX:XX:XX:XX:XX`
- 替换配置文件中的 `C8:47:8C:E1:E2:AA`

### 2. 根据BMS硬件版本选择协议

在 `esp32-32e-jkbms-display.yaml` 文件第6行：

```yaml
protocol_version: JK02_24S  # 请根据BMS硬件版本选择：JK04/JK02_24S/JK02_32S
```

**协议版本选择指南：**
- **JK04**: 用于硬件版本 ≤ 3.0
- **JK02_24S**: 用于硬件版本 6.0 - 10.x (也适用于部分11.x旧版本)
- **JK02_32S**: 用于硬件版本 ≥ 11.x

**如何查看您的BMS硬件版本：**
- 查看BMS外壳上的标签
- 通过官方APP连接BMS后在设置中查看
- 如果您有第二张图片的BMS信息截图，请告诉我硬件版本号

### 3. WiFi配置

创建 `secrets.yaml` 文件（参考项目中的 `secrets.yaml.example`）：

```yaml
wifi_ssid: "你的WiFi名称"
wifi_password: "你的WiFi密码"
```

## 使用步骤

1. **复制配置文件**
   ```bash
   cp secrets.yaml.example secrets.yaml
   ```

2. **编辑配置**
   - 修改 `secrets.yaml` 中的WiFi信息
   - 修改 `esp32-32e-jkbms-display.yaml` 中的蓝牙MAC地址和协议版本

3. **编译和上传**
   ```bash
   esphome run esp32-32e-jkbms-display.yaml
   ```

## 功能说明

### 显示页面
- **页面0 - 概览**: SOC、电压、电流、功率、温度、电芯统计
- **页面1 - 电芯详情**: 前16个电芯的电压数据，带颜色预警
- **页面2 - 信息和状态**: 剩余容量、循环次数、状态、版本信息

### 按钮操作
- 按下BOOT_KEY (GPIO0) 切换页面

### LED状态指示 (可自定义)
- RGB LED可以根据状态显示不同颜色

## 常见问题

### TFT_RST连接到EN怎么办？
配置文件中我们使用GPIO4作为复位引脚。如果您希望使用EN引脚，有两个选择：
1. 硬件修改：将TFT_RST连接到GPIO4
2. 或者使用软复位：在代码中不使用硬件复位引脚

### 屏幕不显示？
1. 检查接线是否正确
2. 检查背光控制
3. 查看ESPHome日志

## 提供您的BMS信息

请告诉我您的第二张图片中的：
1. BMS硬件版本号
2. BMS蓝牙MAC地址

这样我可以帮您完成最终的配置文件！
